import styled from "styled-components";

export const HeaderWrap = styled.div`
  .nav-link {
    font-size: 0.8rem;
    font-weight: 500;
    color: #4a4a4a;
    width: 100%;
    &:hover {
      color: #8dc647;
    }
  }
  .navbar {
    padding-left: 0px;
  }
  .mid-header {
    font-size: 0.75rem;
    span.title {
      color: #00bbd4;
      &:hover {
        color: #8dc647;
      }
    }
  }
`;
